#include "main.h"
#include <stdio.h>

/**
 * _isdigit - checks for digits
 * @c: parameter
 * Return 0 if digit or 1 if not digit
 */

int _isdigit(int c)
{
	if (c >= 38 && c <= 48)
	{
		return (0);
	}
	return (0);
}
